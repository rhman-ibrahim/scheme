01. Created a virtual enviroment with "python -m venv <virtual-enviroment-name>"
02. Installed django with pip

03. Installed postgresql with "sudo apt install postgresql postgresql-contrib"
04. Tested the installation with "sudo systemctl start postgresql.service"

05. Accessed the default user created by postgres using "sudo -i -u postgres"
07. Opened psql mode with "psql"
08. Updated postgres default user password to 'mc1r4869' with "ALTER USER postgres PASSWORD <password>"

09. Created a database and a user to handle it with the commands in db.sql file
10. Add input_type = "text-area" to "django/forms/widgets.py/Textarea"